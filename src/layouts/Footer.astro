---
import Logo from '../assets/logo.astro';
import Footer from '../components/Footer.svelte';
import {getData} from '../helpers/dataFetching.js';

const footerData = await getData("menus")
---
<footer>
    <Logo />
    <Footer client:visible footerData={footerData}/>
</footer>

<style></style>